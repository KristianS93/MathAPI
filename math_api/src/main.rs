use fib_squence::add;
use infrastructure::subtract;

fn main() {

    // subtract func
    // add
    let x = add(2, 4);
    let y = subtract(6,2);


    println!("Hello, world! add: {}, subtract {}", x, y);

}
